numpy>=1.24.0,<3.0
scipy>=1.10.0,<2.0
sympy>=1.12
matplotlib>=3.7.0,<4.0
ttkbootstrap>=1.10.0,<2
pylatex>=1.4.2,<2
PyMuPDF>=1.24.0,<2
Pillow>=10.0.0
ezdxf>=1.0,<2
# ─── Opcional: JIT con numba (perf) ────────────────────────────────────────
# @njit acelera los hot paths (FEM core + rasterizado del campo). fem/_numba_compat.py
# provee un fallback que convierte @njit en no-op si numba NO esta instalado: el
# codigo corre igual, solo mas lento. numba se desfasa respecto de numpy (las
# versiones recientes tardan en soportar el ultimo numpy), por eso NO se fija como
# dependencia dura — instalalo aparte si queres el JIT, con una version compatible
# con tu numpy instalado:
#     pip install "numba>=0.60"
# Pesa ~30 MB (incluye LLVM); excluirlo del bundle PyInstaller lo aliviana.
