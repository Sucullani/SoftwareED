numpy>=1.24.0,<3.0
scipy>=1.10.0,<2.0
sympy>=1.12
matplotlib>=3.7.0,<4.0
ttkbootstrap>=1.10.0,<2
pylatex>=1.4.2,<2
PyMuPDF>=1.24.0,<2
Pillow>=10.0.0,<14
ezdxf>=1.0,<2
#
# Binario externo OPCIONAL (no instalable por pip): pdflatex (MiKTeX / TeX Live).
# La Memoria de Cálculo PDF y la teoría usan pylatex -> pdflatex para calidad
# documento. Sin pdflatex la app sigue funcionando (las fórmulas caen al
# fallback mathtext). Instalá MiKTeX/TeX Live aparte si querés el PDF.
#
