# <Título de la tarea>

**Fecha**: AAAA-MM-DD · **Autor**: <persona o agente> · **Estado**: en curso | terminado | bloqueado

## Qué se pedía

Una o dos frases. Si vino de un informe o de una nota previa, enlazala.

## Qué se hizo

Lo que quedó en el repo. Sin repetir el diff: lo que el diff **no** dice.

## Qué se descartó y por qué

Los caminos que se probaron y no funcionaron. Es la parte más valiosa de la nota: evita
que el próximo agente los vuelva a recorrer.

## Trampas encontradas

Comportamientos no evidentes (Tk, matplotlib, PyInstaller, LaTeX, orden de inicialización,
`after` sin cancelar, rutas que cambian dentro del `.exe`…).

## Qué quedó pendiente

Con el nivel de detalle suficiente para que otro lo retome sin preguntar. Si espera una
decisión del autor, decir **cuál** es la decisión.

## Verificación

Qué se corrió y con qué resultado (`python -m tests.test_fem`, arranque de la GUI,
compilación de la tesis…). Si algo no se pudo verificar, decirlo.
